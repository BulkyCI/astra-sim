name: Reusable ns-3 evaluation

on:
  workflow_call:
    inputs:
      evaluation_name:
        description: >
          Human-readable name for the ledger entry. Job names are static:
          the caller's job is already named after the evaluation, and GitHub
          prefixes called jobs with it, so repeating it here only duplicated
          the prefix.
        required: true
        type: string
      profile:
        description: Repository-relative experiment profile.
        required: true
        type: string
      run_directory:
        description: Repository-relative directory for all generated inputs and results.
        required: true
        type: string
      artifact_name:
        description: Artifact prefix; the workflow appends the GitHub run ID.
        required: true
        type: string
      ledger_issue:
        description: Ledger issue number for the calling run; zero disables publication.
        required: false
        default: 0
        type: number
      ledger_key:
        description: Stable ledger section identity, reused across re-runs.
        required: true
        type: string
      release_tag:
        description: >
          Release tag from the calling run's `ledger` job; empty disables
          archiving. Never derived from run state here — that would split a
          partial re-run across two releases. One release per run suffices:
          raw transport streams are hashed and discarded on the runner
          (segment_hasher.py) rather than shipped, so an arm's archive is
          reports, summaries, and the attestation — a handful of assets,
          nowhere near the 1000-asset release cap that once forced
          sharding.
        required: false
        default: ''
        type: string
      comparison:
        description: Run a matched comparison instead of one structural experiment.
        required: true
        type: boolean
      comparison_seed:
        description: Optional single seed for one independently scheduled matched pair; zero runs the default seed set.
        required: false
        default: 0
        type: number
      execution_timeout_minutes:
        description: Wall-clock guard for the run or comparison command.
        required: true
        type: number
      simulation_timeout_seconds:
        description: Wall-clock cap for each ns-3 simulator process.
        required: true
        type: number
      require_congestion:
        description: Fail a paired evaluation without raw queue, PFC, and background-traffic evidence.
        required: true
        type: boolean
      cluster_runtime_artifact:
        description: >
          Artifact-name prefix of the prebuilt cluster runtime bundles from
          the calling run's `cluster-build` job. Each cluster node appends
          the instruction-set level it selects for itself
          (`-x86-64-v3`/`-x86-64-v4`). Unused by the hosted structural path,
          which builds through the shared compiler cache.
        required: true
        type: string

permissions:
  contents: read

jobs:
  evaluate:
    name: Structural experiment
    if: ${{ !inputs.comparison }}
    runs-on: ubuntu-24.04
    # The reusable workflow owns a complete hosted job. Callers reserve their
    # simulator and wrapper budgets below the six-hour GitHub Actions limit.
    timeout-minutes: 360
    # This job runs the compiled simulator for hours. It holds no write scope
    # at all; every outward-facing effect happens in the `archive` job below.
    permissions:
      contents: read
    env:
      EXPERIMENT_PROFILE: ${{ inputs.profile }}
      EXPERIMENT_RUN_DIRECTORY: ${{ inputs.run_directory }}

    steps:
    - name: Check out source and submodules
      uses: actions/checkout@v7
      with:
        submodules: recursive
        persist-credentials: false

    - name: Set up the locked Python environment
      uses: ./.github/actions/python-env

    # Builds its own backends through the shared compiler cache instead of
    # consuming the native-build artifact. Structural experiments run only
    # on dispatch, and paying a cached build here frees every evaluation
    # from gating on - and failing with - a build job whose artifact the
    # cluster path never used.
    - name: Build the native backends through the trusted compiler cache
      uses: ./.github/actions/native-build

    - name: Run structural ns-3 experiment
      env:
        EXECUTION_TIMEOUT_MINUTES: ${{ inputs.execution_timeout_minutes }}
        SIMULATION_TIMEOUT_SECONDS: ${{ inputs.simulation_timeout_seconds }}
      run: |
        timeout --foreground "${EXECUTION_TIMEOUT_MINUTES}m" \
          uv run --locked python experiments/ring_3d/run.py \
          --profile "$EXPERIMENT_PROFILE" \
          --output "$EXPERIMENT_RUN_DIRECTORY" \
          --simulation-timeout-seconds "$SIMULATION_TIMEOUT_SECONDS" \
          --clean

    - name: Publish structural researcher report
      if: ${{ always() }}
      run: |
        uv run --locked python experiments/ring_3d/report.py \
          --profile "$EXPERIMENT_PROFILE" \
          --run-dir "$EXPERIMENT_RUN_DIRECTORY" \
          --output "$EXPERIMENT_RUN_DIRECTORY/research_report.md"
        cat "$EXPERIMENT_RUN_DIRECTORY/research_report.md" >> "$GITHUB_STEP_SUMMARY"

    - name: Upload reproducibility bundle
      if: always()
      uses: actions/upload-artifact@v7
      with:
        name: ${{ inputs.artifact_name }}-${{ github.run_id }}
        path: ${{ inputs.run_directory }}
        if-no-files-found: ignore
        overwrite: true

  provision:
    name: Provision the cluster runner
    # Every paired comparison runs on the cluster: heavy pairs cannot finish
    # under the 6-hour hosted ceiling, and self-hosted jobs may run 5 days.
    if: ${{ inputs.comparison }}
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    # Authenticates with repository secrets, not the workflow token.
    permissions: {}

    steps:
    - name: Check out the provisioning action
      uses: actions/checkout@v7
      with:
        persist-credentials: false
        sparse-checkout: .github/actions/dcs-provision
        sparse-checkout-cone-mode: false

    - name: Mint a just-in-time runner and hand it to SLURM
      uses: ./.github/actions/dcs-provision
      with:
        runner-name: dcs-${{ github.run_id }}-${{ github.run_attempt }}-${{ inputs.ledger_key }}
        # Also the exact label the companion evaluate_cluster job requires.
        # Pinning each runner to its own experiment keeps the assignment
        # 1:1 by construction - a runner can never pick up a concurrent
        # experiment's job - so a SLURM job id always maps to one known
        # Actions run, and per-experiment resource shapes stay possible.
        run-label: dcs-r${{ github.run_id }}-${{ inputs.ledger_key }}
        app-id: ${{ vars.DCS_APP_ID }}
        app-private-key: ${{ secrets.DCS_APP_PRIVATE_KEY }}
        ssh-key: ${{ secrets.DCS_SSH_KEY }}
        ssh-dest: ${{ vars.DCS_SSH_DEST }}
        ssh-host-key: ${{ vars.DCS_SSH_HOST_KEY }}

  evaluate_cluster:
    name: Cluster comparison
    needs: provision
    if: ${{ inputs.comparison }}
    # The run-scoped label pins this job to the one runner its provision
    # job minted: concurrent experiments can never swap runners, so a SLURM
    # job id always identifies one Actions run. (A JIT registration carries
    # exactly the labels passed to generate-jitconfig - the implicit
    # self-hosted/Linux/X64 defaults of config.sh registration never exist.)
    runs-on: dcs-r${{ github.run_id }}-${{ inputs.ledger_key }}
    # Timeout chain, derived from the SLURM walltime in runner-job.sbatch:
    # per-sim 396000 s < comparison 6720 min (both in evaluation-matrix.json)
    # < this job ceiling 6900 min < walltime 6960 min. Each layer leaves the
    # next enough headroom to report and upload; wall clocks are backstops,
    # not budgets - wedges die via the sim's forward-progress deadline.
    timeout-minutes: 6900
    # No write scope, and no GitHub-bound effect at all: raw segments are
    # hashed and discarded on the node (segment_hasher.py), results leave
    # through the scratch outbox, and the freshly tokened courier job
    # below performs the uploads. Nothing in a multi-day arm depends on a
    # credential, so nothing can outlive one - the runner's tokens stop
    # refreshing at 24 hours, which killed the old mid-run uploader.
    permissions:
      contents: read
    env:
      EXPERIMENT_PROFILE: ${{ inputs.profile }}
      EXPERIMENT_RUN_DIRECTORY: ${{ inputs.run_directory }}

    steps:
    - name: Check out source
      uses: actions/checkout@v7
      with:
        # The prebuilt runtime bundle replaces the native tree, so the
        # expensive recursive ns-3 clone never lands on cluster scratch.
        submodules: false
        persist-credentials: false

    - name: Set up the locked Python environment
      uses: ./.github/actions/python-env
      with:
        cache: 'false'

    # The compile node is no longer the run node: cluster-build compiled
    # one bundle per fleet ISA level, and this node picks the highest one
    # its CPU carries. x86-64-v4 is exactly AVX-512 F/BW/CD/DQ/VL; the
    # fallback, x86-64-v3, runs on every node the sbatch constraint
    # admits (AMD_Epyc7453 is the Zen 3 floor).
    - name: Select the instruction-set level this node supports
      id: isa
      run: |
        set -euo pipefail
        march=x86-64-v4
        for flag in avx512f avx512bw avx512cd avx512dq avx512vl; do
          grep -qw "$flag" /proc/cpuinfo || { march=x86-64-v3; break; }
        done
        echo "march=${march}" >> "$GITHUB_OUTPUT"
        echo "selected ${march}"

    # Pure lookup against the shared store the run's seed job published
    # before this wave provisioned. A miss is survivable: the job falls
    # back to a private download and extraction - exactly the pre-store
    # behavior - so sharing stays an optimization layer that correctness
    # never depends on.
    - name: Resolve the shared runtime store
      id: store
      env:
        RUNTIME_KEY: ${{ inputs.cluster_runtime_artifact }}-${{ steps.isa.outputs.march }}
      run: |
        set -euo pipefail
        if entry="$(bash ci/dcs/resolve-runtime.sh "$RUNTIME_KEY")"; then
          echo "shared runtime: ${entry}"
          { echo "hit=true"; echo "entry=${entry}"; } >> "$GITHUB_OUTPUT"
        else
          echo "no shared runtime for ${RUNTIME_KEY}; falling back to a private extraction"
          { echo "hit=false"; echo "entry="; } >> "$GITHUB_OUTPUT"
        fi

    - name: Fetch the prebuilt cluster runtime
      if: ${{ steps.store.outputs.hit != 'true' }}
      uses: actions/download-artifact@v8
      with:
        name: ${{ inputs.cluster_runtime_artifact }}-${{ steps.isa.outputs.march }}

    # Links against the sealed store entry (or unpacks the private
    # bundle), proves the binary's library closure resolves on this node,
    # and exports the runtime environment (ASTRA_SIM_LD_LIBRARY_PATH, the
    # Open MPI relocation variables, and the binary path the attest step
    # hashes) for the later steps.
    - name: Install and verify the cluster runtime
      env:
        MARCH: ${{ steps.isa.outputs.march }}
        STORE_ENTRY: ${{ steps.store.outputs.entry }}
      run: bash ci/dcs/install-runtime.sh "cluster-runtime-${MARCH}.tar.gz"

    # Folds sealed raw-log segments into the arm's stream digest and
    # deletes them, so per-arm scratch stays near two segments instead of
    # the full multi-gigabyte stream. No IPC with the simulator: a segment
    # is sealed exactly when a higher-numbered sibling exists on disk.
    # SIGTERM means "the sim is done - drain everything and exit". No
    # token, no network: its predecessor uploaded segments to the release
    # and died when the runner's token stopped refreshing at 24 hours,
    # after which segments piled up to the scratch quota. A hasher cannot
    # outlive a credential it never holds.
    - name: Start the mid-run segment hasher
      env:
        WATCH_DIR: ${{ inputs.run_directory }}
      run: |
        mkdir -p "$WATCH_DIR"
        # The project venv's interpreter: the synced env provides
        # zstandard for streaming decompression. Direct venv path rather
        # than `uv run` so $! is the hasher itself and the drain step's
        # SIGTERM reaches it. -u because a block-buffered log once hid a
        # dead sidecar for hours.
        nohup .venv/bin/python -u .github/scripts/segment_hasher.py \
          --watch-dir "$WATCH_DIR" \
          > "$RUNNER_TEMP/segment-hasher.log" 2>&1 &
        echo $! > "$RUNNER_TEMP/segment-hasher.pid"

    - name: Run the full paired comparison
      # Inputs cross into the shell as environment data, never as text
      # spliced into the script.
      env:
        EXECUTION_TIMEOUT_MINUTES: ${{ inputs.execution_timeout_minutes }}
        SIMULATION_TIMEOUT_SECONDS: ${{ inputs.simulation_timeout_seconds }}
        REQUIRE_CONGESTION: ${{ inputs.require_congestion }}
        COMPARISON_SEED: ${{ inputs.comparison_seed }}
        # Scoped to this one step, never job-global: the runtime env's
        # conda libssl would shadow the system OpenSSL for every later
        # step's tooling (it silenced the segment uploader's TLS for
        # hours on 2026-08-30). See install-runtime.sh.
        LD_LIBRARY_PATH: ${{ env.ASTRA_SIM_LD_LIBRARY_PATH }}
      run: |
        command=(
          uv run --locked python experiments/ring_3d/compare.py
          --profile "$EXPERIMENT_PROFILE"
          --output "$EXPERIMENT_RUN_DIRECTORY"
          --simulation-timeout-seconds "$SIMULATION_TIMEOUT_SECONDS"
          --clean
        )
        if [[ "$REQUIRE_CONGESTION" == "true" ]]; then
          command+=(--require-congestion)
        fi
        if [[ "$COMPARISON_SEED" != "0" ]]; then
          command+=(--seeds "$COMPARISON_SEED")
        fi
        timeout --foreground "${EXECUTION_TIMEOUT_MINUTES}m" "${command[@]}"

    # The sim has exited, so every segment on disk is final. Drain before
    # the attest step reads the digest, or the stream record would be a
    # mid-run snapshot. Bounded wait; job teardown reaps a straggler.
    - name: Drain and stop the segment hasher
      if: ${{ always() }}
      run: |
        pid="$(cat "$RUNNER_TEMP/segment-hasher.pid" 2>/dev/null)" || exit 0
        if kill -TERM "$pid" 2>/dev/null; then
          for _ in $(seq 1 120); do
            kill -0 "$pid" 2>/dev/null || break
            sleep 5
          done
        fi
        # Past the bounded wait the hasher must be dead, not merely asked:
        # a survivor deleting files while the outbox placement below walks
        # the directory would corrupt the copy. Whatever it had not folded
        # is recorded by the attestation as a partial stream.
        kill -9 "$pid" 2>/dev/null || true
        echo "--- segment hasher log ---"
        cat "$RUNNER_TEMP/segment-hasher.log" 2>/dev/null || true

    # The arm's commitment: binary identity, machine facts, the stream
    # digest the hasher folded, and a sha256 for every retained output.
    # Runs after the drain (the digest is final) and before the report
    # publication and outbox placement (both must carry the amended
    # report). MARCH and RUNTIME_KEY mirror the install step's inputs;
    # ASTRA_SIM_BINARY was exported by install-runtime.sh.
    - name: Attest the arm's provenance
      if: ${{ always() }}
      env:
        MARCH: ${{ steps.isa.outputs.march }}
        RUNTIME_KEY: ${{ inputs.cluster_runtime_artifact }}-${{ steps.isa.outputs.march }}
        LEDGER_KEY: ${{ inputs.ledger_key }}
        COMPARISON_SEED: ${{ inputs.comparison_seed }}
      run: |
        .venv/bin/python .github/scripts/attest.py \
          --run-dir "$EXPERIMENT_RUN_DIRECTORY" \
          --report "$EXPERIMENT_RUN_DIRECTORY/comparison_report.md"

    - name: Publish paired comparison report
      if: ${{ always() }}
      run: |
        if [[ -f "$EXPERIMENT_RUN_DIRECTORY/comparison_report.md" ]]; then
          cat "$EXPERIMENT_RUN_DIRECTORY/comparison_report.md" >> "$GITHUB_STEP_SUMMARY"
        else
          echo "Paired evaluation did not produce a comparison report; inspect the retained diagnostics." >> "$GITHUB_STEP_SUMMARY"
        fi

    # The arm's only delivery: rename the results onto shared scratch for
    # the courier job. Uploading from here is not an option - by the time
    # a multi-day arm finishes, every token this job ever held has been
    # dead for days (the runner refreshes them for at most 24 hours), so
    # anything GitHub-bound must happen in a job that starts after this
    # one ends. The outbox excludes raw segments by construction; the
    # attestation carries their hashes instead.
    - name: Place the results in the courier outbox
      if: ${{ always() }}
      env:
        LEDGER_KEY: ${{ inputs.ledger_key }}
      run: |
        bash ci/dcs/outbox.sh place \
          "$GITHUB_RUN_ID" "$LEDGER_KEY" "$EXPERIMENT_RUN_DIRECTORY"

  provision_courier:
    name: Provision the courier runner
    # The collection/cleanup half of the arm: runners refresh their tokens
    # for at most 24 hours, so a multi-day arm cannot upload anything -
    # its results wait in the scratch outbox, and this freshly minted
    # runner (whose job starts now, so its tokens are minutes old) is the
    # only path they take to GitHub. Runs on failure too: a failed arm's
    # partial outbox is exactly the forensic record worth collecting.
    needs: evaluate_cluster
    if: ${{ !cancelled() && inputs.comparison }}
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    # Authenticates with repository secrets, not the workflow token.
    permissions: {}

    steps:
    - name: Check out the provisioning action
      uses: actions/checkout@v7
      with:
        persist-credentials: false
        sparse-checkout: .github/actions/dcs-provision
        sparse-checkout-cone-mode: false

    - name: Mint a just-in-time runner and hand it to SLURM
      uses: ./.github/actions/dcs-provision
      with:
        runner-name: dcs-${{ github.run_id }}-${{ github.run_attempt }}-${{ inputs.ledger_key }}-courier
        run-label: dcs-r${{ github.run_id }}-${{ inputs.ledger_key }}-courier
        app-id: ${{ vars.DCS_APP_ID }}
        app-private-key: ${{ secrets.DCS_APP_PRIVATE_KEY }}
        ssh-key: ${{ secrets.DCS_SSH_KEY }}
        ssh-dest: ${{ vars.DCS_SSH_DEST }}
        ssh-host-key: ${{ vars.DCS_SSH_HOST_KEY }}

  courier:
    name: Collect the results from the cluster
    needs: provision_courier
    if: ${{ !cancelled() && needs.provision_courier.result == 'success' }}
    # A queued self-hosted job is cancelled by GitHub after 24 hours, so a
    # pathological SLURM queue can strand this collection. That is a
    # recoverable state, not a loss: the outbox waits on scratch for at
    # least five days (outbox.sh places into the freshest generation), the
    # missing report keeps the ledger issue open and fails the aggregate's
    # exact-count gate, and "re-run failed jobs" provisions a new courier
    # that finds the outbox where the arm left it.
    runs-on: dcs-r${{ github.run_id }}-${{ inputs.ledger_key }}-courier
    # Minutes of work (locate, artifact upload of a report-sized bundle,
    # discard) on a full-shape SLURM allocation: the sbatch script has one
    # resource shape, and a second shape is not worth a second cluster
    # deployment path for a job this short.
    timeout-minutes: 60
    permissions:
      contents: read

    steps:
    - name: Check out the outbox tool
      uses: actions/checkout@v7
      with:
        persist-credentials: false
        sparse-checkout: ci/dcs
        sparse-checkout-cone-mode: false

    - name: Locate the arm's outbox
      id: outbox
      env:
        LEDGER_KEY: ${{ inputs.ledger_key }}
      run: |
        set -euo pipefail
        if path="$(bash ci/dcs/outbox.sh locate "$GITHUB_RUN_ID" "$LEDGER_KEY")"; then
          { echo "hit=true"; echo "path=${path}"; } >> "$GITHUB_OUTPUT"
        else
          # A missing outbox means the arm died before placing results;
          # the arm's own red status already tells that story, and the
          # ledger will record the absent report.
          echo "hit=false" >> "$GITHUB_OUTPUT"
          echo "No outbox was placed for this arm; nothing to collect." >> "$GITHUB_STEP_SUMMARY"
        fi

    - name: Upload the reproducibility bundle
      if: ${{ steps.outbox.outputs.hit == 'true' }}
      uses: actions/upload-artifact@v7
      with:
        name: ${{ inputs.artifact_name }}-${{ github.run_id }}
        path: ${{ steps.outbox.outputs.path }}
        if-no-files-found: ignore
        overwrite: true

    # The sweep aggregations fold exactly one file from this run - the
    # comparison verdict - and must never pay for it by downloading the
    # full bundle. Publish the verdict as its own kilobyte-sized artifact;
    # the aggregate download patterns match only these `-summary-` names.
    - name: Upload the comparison summary for aggregation
      if: ${{ steps.outbox.outputs.hit == 'true' }}
      uses: actions/upload-artifact@v7
      with:
        name: ${{ inputs.artifact_name }}-summary-${{ github.run_id }}
        path: ${{ steps.outbox.outputs.path }}/comparison.json
        if-no-files-found: ignore
        overwrite: true

    # Only after both uploads succeeded: a failed courier re-run must
    # still find the outbox where the arm left it.
    - name: Discard the collected outbox
      env:
        LEDGER_KEY: ${{ inputs.ledger_key }}
      run: bash ci/dcs/outbox.sh discard "$GITHUB_RUN_ID" "$LEDGER_KEY"

  archive:
    name: Record the result
    # The courier edge orders this job after the artifacts exist on the
    # cluster path; the hosted structural path uploads its own.
    needs: [ evaluate, evaluate_cluster, courier ]
    if: ${{ always() }}
    runs-on: ubuntu-24.04
    # Sized for a report-scale bundle: raw transport streams are hashed
    # and discarded on the runner, never shipped, so the 38.8 GB bundles
    # that once forced a 360-minute ceiling here cannot recur. Hosted
    # structural bundles (dispatch-only) may still carry some raw stream.
    timeout-minutes: 120
    # The only job here that holds write scopes. It runs `gh` and `tar` over a
    # downloaded bundle and never executes anything from it, so a malicious
    # artifact cannot reach the token.
    permissions:
      contents: write
      issues: write

    steps:
    - name: Check out the publishing tools
      uses: actions/checkout@v7
      with:
        persist-credentials: false
        sparse-checkout: |
          .github/actions
          .github/scripts
        sparse-checkout-cone-mode: false

    - name: Fetch the evaluation report
      continue-on-error: true
      uses: actions/download-artifact@v8
      with:
        name: ${{ inputs.artifact_name }}-${{ github.run_id }}
        path: ${{ inputs.run_directory }}

    - name: Persist the evaluation report in the ledger
      uses: ./.github/actions/ledger-publish
      with:
        issue: ${{ inputs.ledger_issue }}
        key: ${{ inputs.ledger_key }}
        title: ${{ inputs.evaluation_name }}
        body-file: ${{ inputs.run_directory }}/${{ inputs.comparison && 'comparison_report.md' || 'research_report.md' }}
        # Exactly one of the two paths ran; report the status of that one.
        status: ${{ inputs.comparison && needs.evaluate_cluster.result || needs.evaluate.result }}

    - name: Archive the reproducibility bundle permanently
      # A ledger failure must not cost the archive: each recording channel
      # is best-effort on its own. Cancellation still stops the job.
      if: ${{ !cancelled() }}
      uses: ./.github/actions/release-archive
      with:
        # The run release, opened by the ledger job before any arm ran.
        tag: ${{ inputs.release_tag }}
        artifact: ${{ inputs.artifact_name }}-${{ github.run_id }}
        asset-name: ${{ inputs.artifact_name }}
        # The report fetch above already landed the whole bundle on this
        # runner; archiving it in place avoids a second ~39 GB download
        # that would double both the wait and the disk footprint.
        source-directory: ${{ inputs.run_directory }}
